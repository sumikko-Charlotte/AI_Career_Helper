# AI 职业助手 - 前端模块

## 📖 功能介绍

这是 AI 职业助手项目的前端模块，基于 Vue 3 + Vite + Element Plus 构建，提供现代化的用户界面，包括：

- 🎯 用户登录/注册
- 💼 职位推荐与投递
- 🤖 AI 模拟面试
- 📊 生涯路径规划
- 🩺 简历诊断与生成
- 🎮 虚拟职业体验
- 👤 用户个人中心
- 🔧 管理员后台

## 🚀 本地运行

### 前置要求

- Node.js >= 16.0.0
- npm >= 7.0.0

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

访问地址：http://localhost:5000

### 构建生产版本

```bash
npm run build
```

构建产物将输出到 `dist/` 目录。

## 📦 部署到 Vercel

### 方式一：通过 Vercel CLI

1. 安装 Vercel CLI：
```bash
npm i -g vercel
```

2. 登录 Vercel：
```bash
vercel login
```

3. 部署：
```bash
vercel
```

### 方式二：通过 GitHub 集成

1. 将代码推送到 GitHub 仓库

2. 访问 [Vercel Dashboard](https://vercel.com/dashboard)

3. 点击 "New Project"，导入你的 GitHub 仓库

4. Vercel 会自动检测到 `vercel.json` 配置，直接点击 "Deploy" 即可

### 环境变量配置

如果后端 API 地址需要配置，可在 Vercel 项目设置中添加环境变量：

- `VITE_API_BASE_URL`: 后端 API 基础地址（如：https://your-backend.herokuapp.com）

修改 `vite.config.js` 中的 `proxy` 配置或使用环境变量。

## 📁 项目结构

```
ai-career-helper-frontend/
├── public/          # 静态资源
├── src/
│   ├── assets/     # 图片等资源
│   ├── components/ # Vue 组件
│   ├── router/     # 路由配置
│   ├── utils/      # 工具函数
│   ├── views/      # 页面视图
│   └── main.js     # 入口文件
├── index.html
├── package.json
├── vite.config.js
└── vercel.json     # Vercel 部署配置
```

## 🔧 依赖说明

### 核心依赖

- `vue@^3.4.0` - Vue 3 框架
- `vue-router@^4.3.0` - 路由管理
- `element-plus@^2.13.1` - UI 组件库
- `axios@^1.7.2` - HTTP 请求库
- `echarts@^6.0.0` - 图表库

### 开发依赖

- `vite@^7.3.1` - 构建工具
- `@vitejs/plugin-vue` - Vue 插件

## 📝 注意事项

1. **API 地址配置**：部署前请确保 `vite.config.js` 中的 `proxy` 配置或环境变量指向正确的后端地址

2. **构建优化**：生产环境构建会自动进行代码压缩和优化

3. **浏览器兼容性**：支持现代浏览器（Chrome、Firefox、Safari、Edge 最新版本）

## 🆘 常见问题

**Q: 部署后页面空白？**  
A: 检查 `vercel.json` 中的 `rewrites` 配置是否正确，确保所有路由都指向 `index.html`

**Q: API 请求失败？**  
A: 检查后端服务是否正常运行，以及 CORS 配置是否正确

## 📄 许可证

本项目仅供学习使用。
