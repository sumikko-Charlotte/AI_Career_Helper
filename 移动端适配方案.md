# 手机端适配方案

## 当前问题
- 手机页面压缩不适配
- 侧边栏在手机上占用空间
- 内容区域被压缩
- 字体和按钮在小屏幕上显示异常

## 解决方案

### 1. 技术方案
- **使用 vw/vh 单位**：替代固定 px，实现响应式
- **隐藏侧边栏**：手机端默认隐藏，通过汉堡菜单呼出
- **全屏主内容区**：手机端主内容区占满屏幕
- **防止横向滚动**：使用 `max-width: 100vw` 和 `overflow-x: hidden`
- **字体自适应**：使用 vw 单位，确保在 375px-414px 屏幕上清晰可读

### 2. 修改文件
只需要修改 **`ai-career-helper-frontend/src/App.vue`** 一个文件

### 3. 具体修改步骤

#### 步骤 1：添加汉堡菜单按钮（HTML）
在 `<el-header class="topbar">` 内部，添加汉堡菜单按钮：

```vue
<el-header class="topbar">
  <!-- 汉堡菜单按钮（手机端显示） -->
  <button class="mobile-menu-toggle" @click="isSidebarOpen = !isSidebarOpen">
    ☰
  </button>
  <div class="topbar-left">
    <!-- 原有内容 -->
  </div>
</el-header>
```

#### 步骤 2：添加 JavaScript 控制逻辑
在 `<script setup>` 中添加：

```javascript
// 手机端侧边栏开关控制
const isSidebarOpen = ref(false)

// 监听侧边栏状态，动态添加/移除 body 类名
watch(isSidebarOpen, (newVal) => {
  if (newVal) {
    document.body.classList.add('sidebar-open')
  } else {
    document.body.classList.remove('sidebar-open')
  }
})
```

#### 步骤 3：添加移动端 CSS 样式
在 `<style scoped>` 的最后，添加以下代码：

```css
/* ====================================== */
/* 手机端布局适配（只在手机上生效） */
/* ====================================== */
@media (max-width: 768px) {
  /* 1. 基础布局：防止横向滚动 */
  .app-shell,
  .page,
  body,
  html {
    width: 100vw;
    max-width: 100vw;
    overflow-x: hidden;
    box-sizing: border-box;
  }

  /* 2. 侧边栏：默认隐藏，点击后滑入 */
  .app-aside,
  :deep(.el-aside),
  .side-menu,
  .brand,
  .aside-footer {
    position: fixed;
    top: 0;
    left: -100%; /* 默认滑出屏幕外 */
    width: 75vw !important;
    height: 100vh !important;
    z-index: 9999;
    transition: left 0.3s ease;
    box-shadow: 2px 0 8px rgba(0, 0, 0, 0.15);
  }

  /* 3. 侧边栏打开时的状态 */
  body.sidebar-open .app-aside,
  body.sidebar-open :deep(.el-aside),
  body.sidebar-open .side-menu {
    left: 0; /* 滑入屏幕 */
  }

  /* 4. 主内容区：全屏显示 */
  .app-main,
  :deep(.el-main) {
    width: 100% !important;
    margin-left: 0 !important;
    padding: 3vw !important;
    min-width: 0 !important;
  }

  /* 5. Element Plus 容器适配 */
  :deep(.el-container) {
    flex-direction: column !important;
    overflow-x: hidden !important;
  }

  /* 6. 顶部栏适配 */
  .topbar {
    width: 100% !important;
    padding: 3vw 4vw !important;
    min-height: 12vw !important;
    display: flex;
    align-items: center;
    gap: 3vw;
  }

  /* 7. 汉堡按钮样式（只在手机上显示） */
  .mobile-menu-toggle {
    display: block !important;
    font-size: 6vw;
    background: none;
    border: none;
    color: #333;
    cursor: pointer;
    padding: 2vw;
    line-height: 1;
    flex-shrink: 0;
  }
  
  .mobile-menu-toggle:hover {
    opacity: 0.7;
  }

  /* 8. 侧边栏打开时的遮罩层 */
  body.sidebar-open::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.5);
    z-index: 9998;
    animation: fadeIn 0.3s ease;
  }

  /* 9. 页面内容适配 */
  .page {
    padding: 3vw !important;
    width: 100% !important;
    max-width: 100vw !important;
    box-sizing: border-box;
  }

  /* 10. 字体大小适配（使用 vw 单位） */
  .topbar-title {
    font-size: 4.5vw !important;
  }

  .topbar-tag {
    font-size: 3vw !important;
  }

  /* 11. 按钮和输入框适配 */
  button,
  .el-button {
    font-size: 3.5vw !important;
    padding: 2vw 4vw !important;
  }

  input,
  .el-input__inner {
    font-size: 3.5vw !important;
    padding: 2vw 3vw !important;
  }
}

/* 电脑端：隐藏汉堡按钮 */
@media (min-width: 769px) {
  .mobile-menu-toggle {
    display: none !important;
  }
  
  /* 确保电脑端布局不受影响 */
  .app-aside,
  :deep(.el-aside) {
    position: relative !important;
    left: auto !important;
    width: 260px !important;
  }
  
  .app-main,
  :deep(.el-main) {
    margin-left: 0 !important;
  }
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
```

### 4. 部署说明

#### Vercel 部署
1. **无需额外配置**：Vercel 会自动识别 Vue 项目
2. **构建命令**：`npm run build`（已在 vercel.json 中配置）
3. **输出目录**：`dist`（已在 vercel.json 中配置）
4. **环境变量**：如有需要，在 Vercel 控制台配置

#### 测试步骤
1. 本地测试：使用浏览器开发者工具切换到手机视图（375px-414px）
2. 部署测试：推送到 main 分支，Vercel 会自动部署
3. 真机测试：在手机上访问部署后的 URL

### 5. 注意事项

1. **只修改 App.vue**：不要修改其他组件文件
2. **保留原有逻辑**：只添加移动端样式，不改动业务逻辑
3. **电脑端不变**：确保 `@media (min-width: 769px)` 中恢复电脑端布局
4. **测试兼容性**：在不同尺寸的手机上测试（375px, 414px 等）

### 6. Git 提交

```bash
git add ai-career-helper-frontend/src/App.vue
git commit -m "feat: 添加手机端自适应布局 - 隐藏侧边栏、全屏主内容、汉堡菜单"
git push origin main
```

Vercel 会自动检测到推送并重新部署。
