# Render 环境变量配置指南

## 📋 环境变量清单

根据代码分析，以下环境变量需要在 Render 中配置：

---

## ✅ 必填环境变量（数据库配置）

### 1. `DB_HOST`
- **说明：** 数据库主机地址
- **来源：** `backend/db_config.py` 第 27 行
- **示例值：** `bj-cynosdbmysql-grp-ovt0aqds.sql.tencentcdb.com`
- **获取方式：** 腾讯云控制台 → 数据库实例 → 连接信息 → 外网地址

### 2. `DB_PORT`
- **说明：** 数据库端口
- **来源：** `backend/db_config.py` 第 28 行
- **默认值：** `3306`（如果未设置）
- **示例值：** `20603`
- **获取方式：** 腾讯云控制台 → 数据库实例 → 连接信息 → 外网端口

### 3. `DB_USER`
- **说明：** 数据库用户名
- **来源：** `backend/db_config.py` 第 29 行
- **示例值：** `root`
- **获取方式：** 数据库用户名（通常是 `root`）

### 4. `DB_PASSWORD`
- **说明：** 数据库密码
- **来源：** `backend/db_config.py` 第 30 行
- **示例值：** `你的数据库密码`
- **获取方式：** 腾讯云控制台 → 数据库实例 → 账号管理 → root 密码

### 5. `DB_NAME`
- **说明：** 数据库名称
- **来源：** `backend/db_config.py` 第 31 行
- **示例值：** `ai_career_helper`
- **获取方式：** 数据库名称（在数据库中创建）

---

## ⚪ 可选环境变量

### 6. `DB_CHARSET`
- **说明：** 数据库字符集
- **来源：** `backend/db_config.py` 第 32 行
- **默认值：** `utf8mb4`
- **示例值：** `utf8mb4`
- **是否必填：** 否（有默认值）

### 7. `DB_SSL`
- **说明：** 是否启用 SSL 连接
- **来源：** `backend/db_config.py` 第 41 行
- **默认值：** `false`
- **示例值：** `false` 或 `true`
- **是否必填：** 否（默认不启用 SSL）

### 8. `DEEPSEEK_API_KEY`
- **说明：** DeepSeek API 密钥（用于 AI 功能）
- **来源：** `backend/main.py` 第 60 行
- **默认值：** `sk-d3a066f75e744cd58708b9af635d3606`（代码中有硬编码默认值）
- **示例值：** `sk-你的DeepSeek密钥`
- **是否必填：** 建议配置（避免使用硬编码密钥）
- **获取方式：** [DeepSeek 官网](https://platform.deepseek.com/) → API Keys

### 9. `FRONTEND_ORIGIN`
- **说明：** 前端域名（用于 CORS）
- **来源：** `backend/main.py` 第 40 行
- **默认值：** 空字符串（如果未设置）
- **示例值：** `https://your-frontend-domain.com`
- **是否必填：** 否（CORS 已支持 `*.vercel.app` 域名）

### 10. `RESUME_DOCTOR_URL`
- **说明：** 简历医生服务地址
- **来源：** `backend/main.py` 第 365 行
- **默认值：** `https://ai-career-apper-resume-doctor-69etycfa4ohbkxndweoawk.streamlit.app`
- **示例值：** `https://your-streamlit-app.streamlit.app`
- **是否必填：** 否（有默认值）

---

## 📝 完整配置示例

在 Render Dashboard → Your Service → Environment 中添加以下变量：

```bash
# ==========================================
# 数据库配置（必填）
# ==========================================
DB_HOST=bj-cynosdbmysql-grp-ovt0aqds.sql.tencentcdb.com
DB_PORT=20603
DB_USER=root
DB_PASSWORD=你的数据库密码
DB_NAME=ai_career_helper

# ==========================================
# 数据库配置（可选）
# ==========================================
DB_CHARSET=utf8mb4
DB_SSL=false

# ==========================================
# AI 服务配置（建议配置）
# ==========================================
DEEPSEEK_API_KEY=sk-你的DeepSeek密钥

# ==========================================
# 其他配置（可选）
# ==========================================
FRONTEND_ORIGIN=https://your-frontend-domain.com
RESUME_DOCTOR_URL=https://ai-career-apper-resume-doctor-69etycfa4ohbkxndweoawk.streamlit.app
```

---

## 🔍 代码位置参考

### 数据库配置
- **文件：** `backend/db_config.py`
- **行号：** 第 27-41 行

### DeepSeek API 配置
- **文件：** `backend/main.py`
- **行号：** 第 60 行

### CORS 配置
- **文件：** `backend/main.py`
- **行号：** 第 40 行

### 简历医生配置
- **文件：** `backend/main.py`
- **行号：** 第 365 行

---

## ⚠️ 重要提示

1. **数据库密码安全：**
   - 不要在代码中硬编码密码
   - 使用 Render 环境变量存储敏感信息

2. **DeepSeek API Key：**
   - 代码中有硬编码默认值，但建议在 Render 中配置
   - 避免将 API Key 提交到 Git

3. **数据库白名单：**
   - 确保腾讯云数据库白名单包含 Render 的出站 IP
   - 或临时开放 `0.0.0.0/0`（仅用于测试）

4. **环境变量优先级：**
   - Render 环境变量 > `.env` 文件
   - 确保在 Render 中正确配置所有必填变量

---

## 📋 快速配置清单

### 最小配置（必填）

```bash
DB_HOST=bj-cynosdbmysql-grp-ovt0aqds.sql.tencentcdb.com
DB_PORT=20603
DB_USER=root
DB_PASSWORD=你的数据库密码
DB_NAME=ai_career_helper
```

### 推荐配置（包含可选项）

```bash
DB_HOST=bj-cynosdbmysql-grp-ovt0aqds.sql.tencentcdb.com
DB_PORT=20603
DB_USER=root
DB_PASSWORD=你的数据库密码
DB_NAME=ai_career_helper
DB_CHARSET=utf8mb4
DB_SSL=false
DEEPSEEK_API_KEY=sk-你的DeepSeek密钥
```

---

**配置完成后，重启 Render 服务使环境变量生效。**
