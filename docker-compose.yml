services:
  backend:
    build: ./backend
    container_name: ai_backend
    ports:
      - "8000:8000"

  frontend:
    image: nginx:alpine
    container_name: ai_frontend
    volumes:
      - ./frontend/dist:/usr/share/nginx/html:ro
    ports:
      - "80:80"
