# AI 职业助手项目拆分完成报告

## 📋 拆分概览

项目已成功拆分为 3 个独立部署的模块：

1. **前端模块** (`ai-career-helper-frontend`) - Vue 3 + Vite
2. **后端模块** (`ai-career-helper-backend`) - FastAPI
3. **简历医生模块** (`ai-career-helper-resume-doctor`) - Streamlit

---

## 📁 完整目录结构

### 1. 前端模块 (ai-career-helper-frontend)

```
ai-career-helper-frontend/
├── public/                    # 静态资源
│   ├── assets/               # 资源文件
│   ├── images/              # 图片资源
│   ├── *.mp4                # 视频文件
│   └── vite.svg             # 图标
├── src/                      # 源代码
│   ├── assets/              # 资源文件
│   │   └── images/
│   ├── components/          # Vue 组件
│   │   ├── AdminLayout.vue
│   │   ├── CareerExperience.vue
│   │   ├── DigitalHuman.vue
│   │   ├── HistoryRecord.vue
│   │   ├── Login.vue
│   │   ├── ResumeDoctor.vue
│   │   ├── ResumeTemplates.vue
│   │   ├── SloganPage.vue
│   │   ├── UserProfile.vue
│   │   └── VirtualExperiment.vue
│   ├── router/              # 路由配置
│   │   └── index.js
│   ├── utils/              # 工具函数
│   │   └── request.js
│   ├── views/              # 页面视图
│   │   ├── AboutUs.vue
│   │   ├── ExploreGuide.vue
│   │   ├── PrivacyPolicy.vue
│   │   └── admin/          # 管理员页面
│   │       ├── AdminGuide.vue
│   │       ├── AdminProfile.vue
│   │       ├── Dashboard.vue
│   │       ├── PromptConfig.vue
│   │       ├── ResumeTasks.vue
│   │       └── UserManage.vue
│   ├── App.vue             # 根组件
│   ├── main.js            # 入口文件
│   └── style.css          # 样式文件
├── index.html              # HTML 模板
├── package.json           # 依赖配置
├── package-lock.json      # 锁定版本
├── vite.config.js         # Vite 配置
├── vercel.json            # Vercel 部署配置
└── README.md              # 说明文档
```

### 2. 后端模块 (ai-career-helper-backend)

```
ai-career-helper-backend/
├── services/               # 业务逻辑模块
│   ├── ai_advisor.py      # AI 服务
│   └── resume_parser.py    # 简历解析
├── static/                # 静态文件
│   └── avatars/           # 用户头像目录
├── data/                  # 数据文件目录（运行时生成）
├── main.py                # FastAPI 应用入口
├── db_config.py           # 数据库配置
├── requirements.txt        # Python 依赖
├── Procfile               # Render 启动配置
├── runtime.txt            # Python 版本
└── README.md              # 说明文档
```

### 3. 简历医生模块 (ai-career-helper-resume-doctor)

```
ai-career-helper-resume-doctor/
├── .streamlit/            # Streamlit 配置目录
│   └── config.toml        # Streamlit 配置
├── streamlit_app.py       # Streamlit 主应用
├── resume_parser.py       # PDF 解析模块
├── ai_advisor.py          # AI 服务模块
├── requirements.txt       # Python 依赖
└── README.md              # 说明文档
```

---

## 🚀 部署操作指引

### 第一步：创建 GitHub 仓库

为每个模块创建独立的 GitHub 仓库：

#### 1.1 创建前端仓库

```bash
cd ai-career-helper-frontend
git init
git add .
git commit -m "Initial commit: AI Career Helper Frontend"
```

在 GitHub 上创建新仓库 `ai-career-helper-frontend`，然后：

```bash
git remote add origin https://github.com/your-username/ai-career-helper-frontend.git
git branch -M main
git push -u origin main
```

#### 1.2 创建后端仓库

```bash
cd ../ai-career-helper-backend
git init
git add .
git commit -m "Initial commit: AI Career Helper Backend"
```

在 GitHub 上创建新仓库 `ai-career-helper-backend`，然后：

```bash
git remote add origin https://github.com/your-username/ai-career-helper-backend.git
git branch -M main
git push -u origin main
```

#### 1.3 创建简历医生仓库

```bash
cd ../ai-career-helper-resume-doctor
git init
git add .
git commit -m "Initial commit: AI Career Helper Resume Doctor"
```

在 GitHub 上创建新仓库 `ai-career-helper-resume-doctor`，然后：

```bash
git remote add origin https://github.com/your-username/ai-career-helper-resume-doctor.git
git branch -M main
git push -u origin main
```

### 第二步：部署前端到 Vercel

1. **访问 Vercel**
   - 打开 https://vercel.com
   - 使用 GitHub 账号登录

2. **导入项目**
   - 点击 "New Project"
   - 选择 `ai-career-helper-frontend` 仓库
   - Framework Preset: 选择 "Vite"
   - Root Directory: `./`（默认）
   - Build Command: `npm run build`（自动检测）
   - Output Directory: `dist`（自动检测）

3. **配置环境变量**（如需要）
   - 在项目设置中添加 `VITE_API_BASE_URL`（后端 API 地址）

4. **部署**
   - 点击 "Deploy"
   - 等待构建完成
   - 获得部署 URL（如：`https://ai-career-helper-frontend.vercel.app`）

### 第三步：部署后端到 Render

1. **访问 Render**
   - 打开 https://dashboard.render.com
   - 使用 GitHub 账号登录（学生可用免费计划）

2. **创建 Web Service**
   - 点击 "New +" → "Web Service"
   - 选择 `ai-career-helper-backend` 仓库

3. **配置服务**
   - **Name**: `ai-career-helper-backend`
   - **Environment**: `Python 3`
   - **Region**: 选择离你最近的区域
   - **Branch**: `main`
   - **Root Directory**: `./`（默认）
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `uvicorn main:app --host 0.0.0.0 --port $PORT`
   - **Plan**: 选择 **Free**（免费，适合学生）

4. **配置环境变量**
   - 在 "Environment" 标签页添加：
     - `DEEPSEEK_API_KEY`: 你的 DeepSeek API Key
     - `PORT`: Render 会自动设置（无需手动配置）

5. **部署**
   - 点击 "Create Web Service"
   - 等待部署完成（约 5-10 分钟）
   - 获得部署 URL（如：`https://ai-career-helper-backend.onrender.com`）

6. **更新前端 API 地址**
   - 在 Vercel 项目设置中更新 `VITE_API_BASE_URL` 环境变量
   - 或修改前端 `vite.config.js` 中的 `proxy` 配置

### 第四步：部署简历医生到 Streamlit Cloud

1. **访问 Streamlit Cloud**
   - 打开 https://share.streamlit.io
   - 使用 GitHub 账号登录

2. **创建新应用**
   - 点击 "New app"

3. **配置应用**
   - **Repository**: 选择 `ai-career-helper-resume-doctor`
   - **Branch**: `main`
   - **Main file path**: `streamlit_app.py`
   - **App URL**: 自定义（如：`ai-career-resume-doctor`）

4. **配置 Secrets（环境变量）**
   - 点击 "Advanced settings"
   - 在 "Secrets" 中添加：
     ```toml
     DEEPSEEK_API_KEY = "your_deepseek_api_key_here"
     ```

5. **部署**
   - 点击 "Deploy"
   - 等待部署完成（约 2-5 分钟）
   - 获得部署 URL（如：`https://ai-career-resume-doctor.streamlit.app`）

---

## 💰 预算说明

### 免费方案（学生可用）

- **Vercel**: 免费计划，无限部署，适合前端
- **Render**: 免费计划，适合后端（有休眠限制，但可接受）
- **Streamlit Cloud**: 完全免费，适合 Streamlit 应用

**总成本：¥0** ✅

### 可选升级（如需要）

- **Render Pro**: $7/月（去除休眠限制）
- **Vercel Pro**: $20/月（更多功能）

---

## 🔧 后续配置建议

### 1. 数据库配置

后端模块需要连接数据库，建议：

- 使用云数据库（如腾讯云 TDSQL-C、阿里云 RDS）
- 或使用 Render 的 PostgreSQL（免费计划提供）

### 2. CORS 配置

部署后，需要更新后端的 CORS 配置：

在 `main.py` 中修改：
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://your-frontend.vercel.app",  # 前端地址
        "http://localhost:5000"  # 本地开发
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### 3. 环境变量管理

- **前端**: 使用 Vercel 环境变量
- **后端**: 使用 Render 环境变量
- **简历医生**: 使用 Streamlit Secrets

---

## 📝 注意事项

1. **API Key 安全**
   - 不要将 API Key 提交到代码仓库
   - 使用环境变量或 Secrets 管理

2. **数据库连接**
   - 确保数据库安全组已开放对应端口
   - 使用环境变量存储数据库密码

3. **文件上传限制**
   - Render 免费计划有文件大小限制
   - 建议使用云存储（如 AWS S3、阿里云 OSS）存储用户上传的文件

4. **域名配置**（可选）
   - Vercel 和 Render 都支持自定义域名
   - 可在项目设置中配置

---

## ✅ 检查清单

部署前请确认：

- [ ] 所有代码已推送到 GitHub
- [ ] 环境变量已配置（API Key、数据库等）
- [ ] 前端 API 地址已更新
- [ ] 后端 CORS 配置已更新
- [ ] 数据库连接正常
- [ ] 所有模块 README 已阅读

---

## 🆘 遇到问题？

1. **查看部署日志**
   - Vercel: 项目页面 → "Deployments" → 点击部署 → "Build Logs"
   - Render: 项目页面 → "Logs" 标签页
   - Streamlit: 应用页面 → "Manage app" → "Logs"

2. **检查环境变量**
   - 确认所有必需的环境变量都已配置

3. **查看文档**
   - 各模块的 README.md 文件包含详细说明

---

## 🎉 完成！

恭喜！你的 AI 职业助手项目已成功拆分为 3 个独立模块，并可以分别部署到：

- ✅ 前端 → Vercel
- ✅ 后端 → Render
- ✅ 简历医生 → Streamlit Cloud

所有部署都在免费计划内，适合学生使用！
